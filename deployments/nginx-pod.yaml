apiVersion: v1
kind: Pod
metadata:
  labels:
  name: nginx-storage
spec:
  containers:
    - image: nginx
      name: nginx-storage
      volumeMounts:
        - mountPath: /app/data
          name: nginx-pod-data
    - image: busybox
      name: busybox
      command: ["/bin/sh", "-c"]
      args: ["sleep 1000"]
      volumeMounts:
        - mountPath: /scratch
          name: nginx-pod-data
  volumes:
    - name: nginx-pod-data
      persistentVolumeClaim:
        claimName: mealie-data
